# OIDC Configuration
# Replace these values with your OIDC provider details

# OIDC issuer URL (e.g., https://authentik.example.com/application/o/fava/)
OIDC_ISSUER_URL=https://your-authentik-domain.com/application/o/your-app/

# OAuth2 Client ID from OIDC provider
OAUTH2_CLIENT_ID=your-client-id

# OAuth2 Client Secret from OIDC provider
OAUTH2_CLIENT_SECRET=your-client-secret

# OAuth2 Proxy redirect URL
# Should match: http(s)://your-domain/oauth2/callback
OAUTH2_PROXY_REDIRECT_URL=https://your-fava-domain.com/oauth2/callback

# Cookie Secret
# Can generate with: `openssl rand -base64 32 | tr '+/' '-_' | tr -d '='`
OAUTH2_PROXY_COOKIE_SECRET=

# Restrict to specific email domains (use * to allow all)
OAUTH2_PROXY_EMAIL_DOMAINS=*
